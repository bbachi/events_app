import { Directive, ElementRef, HostListener } from '@angular/core';

describe('DisablebuttonDirective', () => {
  it('should create an instance', () => {
    //const directive = new DisablebuttonDirective(null,null);
    //expect(directive).toBeTruthy();
  });
});
