import { SignupbuttonDirective } from './signupbutton.directive';

describe('SignupbuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new SignupbuttonDirective(null);
    expect(directive).toBeTruthy();
  });
});
